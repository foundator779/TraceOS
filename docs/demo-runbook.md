# CASE-042 demo runbook

1. Open **Integrations**. Point out which sources are connected and which are demo fixtures.
2. If Cloud Audit Logs is connected, show the matching real audit event in Google Cloud and its `LIVE GOOGLE CLOUD` evidence card.
3. Click **Run evidence replay**. The bounded API action responds immediately, attaches a watermarked synthetic sign-in image, and reuses cached analysis on later clicks.
4. Open **Fleet** to show distinct approved manifests, identities, tools, and scopes.
5. Open **Replay** and click the image card. Follow its visible path from evidence to Gemini observation, verifier correlation, and the final finding. Point out that the unsupported claim stops before the final lane.
6. In Case Overview, show the fail-closed fallback quarantining `EVID-006`, Fleet Integrity rejecting `EVID-019`, and the Reporting Agent’s application-policy raw-evidence deny followed by its approved recovery. Do not call either event managed proof.
7. Open **Hypotheses** and show the audited `UNVERIFIED → VERIFIED` transition.
8. Wait for `WAITING FOR NEW EVIDENCE`, then click **Simulate 7 days later**.
9. Open **Memory** to show generation two restoring the same case’s verified facts and resolving its open question.
10. Open **Report**, then **Training**. Show the independent Gemma verdict, play the cached Veo and Lyria artifacts, point to the non-evidence boundary, and show the cost ledger.
11. Finish with quick Google Cloud proof shots from the submission manifest: revision, OIDC Pub/Sub worker, private training bucket, model operation IDs, and matching hashes.

Presenter language for source honesty:

> This record came from our real Google Cloud demo project through Cloud Audit Logs. The image, endpoint, DNS, browser, text, and later-event records are synthetic fixtures so the forensic scenario remains safe and deterministic. The generated video and audio are downstream training material, never forensic evidence.
